CREATE TABLE CLIENT ( ID int not null primary key generated always as identity (start with 1, increment by 1), NAME VARCHAR(255),    PASSWORD VARCHAR(255),    USERNAME VARCHAR(255) NOT NULL,  USER_TYPE INT);